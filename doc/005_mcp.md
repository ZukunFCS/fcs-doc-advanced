# Using FCS with AI Agents (MCP)

Starting with FCS 26.10, FCS can provide a local Model Context Protocol (MCP) server. An MCP-compatible AI agent can inspect the current FCS session, operate supported workflows, and read the bundled FCS operating guide.
AI agents can directly help you the time consuming part of your tasks. 

## Before You Start

You need:

- FCS 26.10 or later with a valid license, running on the same computer as the AI agent.
- An MCP client that supports Streamable HTTP servers on the local computer.

FCS must remain open and licensed while the agent is working on it. The MCP server listens only on `127.0.0.1`, so it is not available to other computers or to cloud-only agents.

## Enable the MCP Server

1. Launch FCS normally. The procedure is the same for the installed FCS
  executable and for a Python/script launch.
2. Open **Settings** and expand **MCP Server**.
3. Enable **Enable MCP Server**.
4. Keep the default port, `19877`, unless it conflicts with another local
  program.
5. Save the settings.
6. Confirm that the status displays **Listening at
  [http://127.0.0.1:19877/mcp](http://127.0.0.1:19877/mcp)**.

Use the complete URL shown by FCS when configuring a client. If you change
the port, use the updated URL in every client configuration. Port `19876` is
reserved by FCS and cannot be used for MCP.

## Connect an AI Agent

The setup is performed once for each agent. Name the server `fcs` or `FCS`
and use the URL displayed in FCS.

### ChatGPT Desktop and the Codex App

1. Open **Settings > Plugins > MCP**.
2. Select **Add server**.
3. Enter `FCS` as the name and select **Streamable HTTP**.
4. Enter `http://127.0.0.1:19877/mcp` as the URL.
5. Save the server and restart the app when prompted.

This applies to local ChatGPT/Codex clients that support custom MCP servers.
ChatGPT in a web browser cannot connect directly to the FCS loopback address.

### Codex CLI

Run:

```powershell
codex mcp add fcs --url http://127.0.0.1:19877/mcp
```

Use `codex mcp list` to check the connection. Inside Codex, use `/mcp` to view
the connected server and its tools.

### Claude Code

Run:

```powershell
claude mcp add --transport http --scope user fcs http://127.0.0.1:19877/mcp
```

The user scope makes FCS available in future Claude Code sessions on this
computer. Run `claude mcp list` to check the connection.

### Visual Studio Code and GitHub Copilot

1. Open the Command Palette with **Ctrl+Shift+P**.
2. Run **MCP: Add Server**.
3. Select **HTTP**, enter the FCS URL, and name the server `FCS`.
4. Choose the global/user profile location so that the connection is available
  outside one workspace.
5. Open Chat and use **Configure Tools** to confirm that the FCS tools are
  available.



### OpenCode

Add a remote MCP server to the OpenCode configuration:

```json
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "servers": {
      "fcs": {
        "type": "remote",
        "url": "http://127.0.0.1:19877/mcp",
        "oauth": false
      }
    }
  }
}
```

FCS does not require OAuth because the endpoint is restricted to the local
computer. Check the MCP server list in OpenCode after saving the configuration.

### Claude Desktop

Claude Desktop custom connectors are intended for remotely reachable servers
and cannot be assumed to reach FCS at `127.0.0.1`. FCS currently does
not ship the separate stdio bridge required by Claude Desktop's traditional
local MCP configuration. Use Claude Code or another local Streamable HTTP
client instead.

## Available Workflows

The public MCP tools cover these areas:

- Application and session status, session creation, opening, inspection, and
saving.
- Video import, library and sequence browsing, and video selection.
- Single and batch processing, job progress, status, and cancellation.
- Export validation, export defaults, and session export.
- Renderer status, connection testing, launch, and scene opening.
- Processing profiles and settings inspection or updates.
- ROM gallery queries, updates, prediction, deletion, and timeline navigation.
- Controller inspection, editing, presets, reordering, synchronization, and
explicit saving.

FCS intentionally exposes a curated set of named tools. License management and
internal application commands are not available through MCP. The exact tool
list is discovered automatically by the client when it connects.

## Example conversations

After connecting, speak to the agent normally. You do not need to memorize
MCP tool names. The agent can also read the FCS operating guide exposed
through MCP before acting. Representative exchanges:

::::{container} mcp-chat

:::{container} mcp-chat-turn mcp-chat-user

**User**

添付した作業リストと `D:/FacialCapture/SampleCapture` 内のデータから、FCS の MCP を用いて
`D:/FacialCapture/SampleProject/Sessions` に作業に必要なセッションをすべて作成し、
動画のインポートも実行してください。
:::

:::{container} mcp-chat-turn mcp-chat-agent

**Agent**

`D:/FacialCapture/SampleProject/Sessions` は存在するが空です。作業リストと照らし合わせると、
`D:/FacialCapture/SampleCapture/Assets/scenes` には `Hero_001` と `Extra_001`、`Side_001`
のシーンしかなく、作業リスト上の `Side_A`、`Side_B`、`Extra_A` 用のシーンがありません。
いくつか確認させてください。

<p class="mcp-chat-q-title"><strong>1. セッションはどのように作成しますか？（1つ選択）</strong></p>
<ul class="mcp-chat-options">
<li class="mcp-choice is-selected"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>アクター別セッション（推奨）</strong> — Alice→<code>Hero_001</code>、Bob→<code>Side_A</code>+<code>Side_B</code>、Carol→<code>Extra_A</code> の計4セッション。存在しないシーンは後で差し替え</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>3セッション（actor=performer）</strong> — Alice/Bob/Carol の各アクターにキャラは先行文字（<code>Hero_001</code> 等）を指定</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><code>Hero_001</code> <strong>のみ</strong> — Alice→<code>Hero_001</code> のみ作成し動画もそれだけインポート</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>自分の回答を入力</strong> — 自由記入</span></li>
</ul>

<p class="mcp-chat-q-title"><strong>2. シーンが存在しないキャラ（<code>Side_A</code>、<code>Side_B</code>、<code>Extra_A</code>）のセッションはどのシーンを指定しますか？（1つ選択）</strong></p>
<ul class="mcp-chat-options">
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><code>Hero_001</code> <strong>のシーンで代用（推奨）</strong> — 試験環境なので <code>Hero_001</code> のシーンを流用し、<code>maya_base</code> は <code>D:/FacialCapture/SampleCapture/Assets</code> を指定</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><code>Extra_001</code> <strong>シーンを指定</strong> — <code>Extra_001_setup_FacialBase.ma</code> を指定</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>シーンを指定する</strong> — 自分でシーンと <code>maya_base</code> を指定する</span></li>
<li class="mcp-choice is-selected"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>自分の回答を入力</strong> — <code>Side_A</code> と <code>Side_B</code> は <code>Side_001</code> に、<code>Extra_A</code> は <code>Extra_001</code> にまとめて進めて</span></li>
</ul>

<p class="mcp-chat-q-title"><strong>3. Maya バージョンは何を指定しますか？（1つ選択）</strong></p>
<ul class="mcp-chat-options">
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>2023（推奨）</strong> — 既存セッションと同じ設定</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>2022</strong> — 2022 を指定</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>2024</strong> — 2024 を指定</span></li>
<li class="mcp-choice is-selected"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>自分の回答を入力</strong> — 対応する Maya シーンから読み取って</span></li>
</ul>

<p class="mcp-chat-q-title"><strong>4. インポートする動画はどのセットですか？（1つ選択）</strong></p>
<ul class="mcp-chat-options">
<li class="mcp-choice is-selected"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><code>01_HMC</code> + <code>04_ROM</code> <strong>（推奨）</strong> — <code>01_HMC\01_Alice</code>、<code>02_Bob</code>、<code>03_Carol</code> の動画 + <code>04_ROM</code> の ROM 動画を各セッションへ。<code>02_Reference</code> / <code>03_4view</code> は除外</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><code>01_HMC</code> <strong>のみ</strong> — <code>01_Alice</code> / <code>Bob</code> / <code>Carol</code> フォルダの mp4 のみ各セッションへ</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>すべて（Reference 含む）</strong> — 01～04 の全 mp4（<code>02_Reference</code>、<code>03_4view_Ref</code> も含む）を該当セッションへインポート</span></li>
<li class="mcp-choice"><span class="mcp-choice-mark" aria-hidden="true"></span><span class="mcp-choice-text"><strong>自分の回答を入力</strong> — 自由記入</span></li>
</ul>

<p class="mcp-chat-answers-done">完了しました。3つの FCS セッションを作成し、対応動画と ROM をインポート済みです。</p>

```{list-table}
:header-rows: 1
:widths: 22 28 50
:align: left

* - Actor / Character
  - Maya scene
  - インポート動画
* - Alice / Hero_001
  - `Hero_001_setup_FacialBase.ma`
  - 19本 + ROM 1本
* - Bob / Side_001
  - `Side_001_setup_FacialBase.ma`
  - 16本 + ROM 1本
* - Carol / Extra_001
  - `Extra_001_setup_FacialBase.ma`
  - 13本 + ROM 1本
```

シーンは `D:/FacialCapture/SampleCapture/Assets`、Maya 2025 で作成しました。
作成先は `D:/FacialCapture/SampleProject/Sessions/FCS/` です。
現在は `Bob / Side_001` セッションを開いた状態です。
:::

:::{container} mcp-chat-turn mcp-chat-user

**User**

`Alice / Hero_001` のセッションに切り替えて、Mayaを開いたりシーンを解析したりして、コントローラの登録を行ってほしい。
適切な region と min/max/default も含めて登録して。
:::

:::{container} mcp-chat-turn mcp-chat-agent

**Agent**

完了しました。

**実施内容 (**`Alice / Hero_001` **セッション)**:

1. **セッション切替** — `Alice / Hero_001` を開きました（動画20本、controller_count 0 からスタート）
2. **Maya シーンからパース** — `Hero_001_setup_FacialBase.ma` から `CTRL_`* ノードを抽出し、
  Metahuman プリセットの命名（例: `CTRL_L_brow_down.ty`）と一致することを確認
3. **登録・保存** — Metahuman プリセット経由で 174 コントローラをリージョン付きで登録し保存

**登録結果**:

- upper: 28行（眉・鼻・首など）
- eyelid: 18行（瞬き・まぶたなど）
- gaze: 5行
- lower: 123行（口・舌・顎・歯）
- 合計 **174行** — すべて min=0.0 / max=1.0 / default=0.0 で保存済み
:::

:::{container} mcp-chat-turn mcp-chat-user

**User**

一部のコントローラは min が -1 だったと思う。確認して修正してほしい。
:::

:::{container} mcp-chat-turn mcp-chat-agent

**Agent**

確認・修正完了です。

**調査結果** — Mayaシーン内の各CTRLノードをcommadPortで解析し、各チャンネルの接続先を判定しました:

- 双方向に動くチャンネル（`tx`、または Up/Down のような逆向きペアに接続される `ty`）はレンジ -1..1 が正しい
- 単一の表情（例: `brow_down` → `browDownL`）に繋がる `ty` は 0..1 のまま

**修正内容** — 双方向駆動の 77 コントローラの `min_value` を `-1.0` に更新して保存しました:

- upper: nose、neck 系など
- eyelid: blink、eyelid 系など
- gaze: `CTRL_L/R_eye.tx/.ty`
- lower: mouth、lips、jaw、teeth、tongue 系など

**検証済み** — `ControllerInfo.yaml`に min_value: -1.0が77行反映、全174行の登録が維持されています。シングル方向のコントローラ（`brow_down`、`jawOpen` など）は適切に 0..1 のままです。
:::

::::

## Review Actions Before Approval

Your MCP client may request approval before a tool runs. Read the proposed
action carefully, particularly before allowing an agent to:

- Delete ROMs or cancel processing jobs.
- Change and save application settings.
- Save a session or controller table.
- Start processing or export files.

Prefer asking the agent to inspect or validate first, summarize the planned
changes, and perform the mutation only after you approve it. Controller edits
remain a working draft until the agent explicitly saves them.

## Troubleshooting



### FCS shows Disabled

Enable **MCP Server** in Settings and save. Merely changing the checkbox without
saving does not apply the setting.

### FCS shows Waiting for a valid license

Complete FCS license activation. The endpoint starts only while the license is
valid.

### FCS shows an error or the port is already in use

Choose another port from `1024` through `65535`, except `19876`, save the
settings, and update the URL in the AI agent.

### The agent cannot connect

Confirm that FCS is still open, its status says **Listening**, and the
client URL exactly matches the status URL, including `/mcp`. A client running
on another computer, in a remote development environment, or only in the cloud
cannot reach the FCS `127.0.0.1` endpoint.

### The agent connects but an action is rejected

Check that the FCS license is still valid. The requested operation may also be
outside the curated public tool set or may require an open session, selected
video, renderer connection, or other FCS state. Ask the agent to inspect
the application and session status before retrying.

## Client Documentation

- [ChatGPT and Codex MCP](https://learn.chatgpt.com/docs/extend/mcp.md)
- [Claude Code MCP](https://code.claude.com/docs/en/mcp)
- [Visual Studio Code MCP servers](https://code.visualstudio.com/docs/agent-customization/mcp-servers)
- [OpenCode MCP servers](https://opencode.ai/v2/docs/mcp-servers)

